# Mitä llm tekee.

## Funktioita:

- luokittele onko työ vai vapaa-ajan sähköposti
- kiireellisyysluokittelu
- Luo vastaus
- Tiivistä viesti

Tarvittavat kirjastot (python 3.10.19)

## Py-torch

pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1
pip install --upgrade transformers bitsandbytes peft accelerate datasets trl

pip install protobuf

pip install SentencePiece

# others

pip install tqdm pandas matplotlib
