# LLM

## Functions:

- Classifies email business/pleasure
- Classifies email's urgency (1-3)
- Creates positive reply
- Creates negative reply
- Creates summary of email

# Required libraries (python 3.10.19)

## Py-torch

```
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

Cuda-version of torch for utilising GPU

```
pip install --upgrade transformers bitsandbytes peft accelerate datasets trl
```

```
pip install protobuf
```

```
pip install SentencePiece
```

# others

```
pip install tqdm
```

tqdm not in use so not neccessary. However it is great package so it is mentioned here for future uses. It provides progress bar for tasks.

```
pip install pandas matplotlib
```

## classifying_test:

```
pip install scikit-learn
pip install matplotlib
pip install seaborn
```

well the matplotlib is here for second time, so it wont install it but it is a nice package so why not mention it twice..
